package com.openbikecomputer.model;

/**
 * All metric kinds tracked by the bike computer. Ported from the
 * {@code MetricType} enum in {@code Metric.ts}.
 */
public enum MetricType {
    ALTITUDE,
    CADENCE,
    DISTANCE,
    GRADE,
    HEART_RATE,
    LATITUDE,
    LONGITUDE,
    POWER_BALENCE,
    POWER,
    SPEED,
    TEMPERATURE,
    VERTICAL_SPEED,
    WHEEL_ROTATIONS,
    BEARING,
    LAP_COUNTER,
    CURRENT_TIME,
    ELAPSED_TIME
}
